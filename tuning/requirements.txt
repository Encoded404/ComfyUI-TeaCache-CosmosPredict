# Requirements for the TeaCache Anima tuning toolkit.
# The ComfyUI environment itself (torch, comfy, etc.) is expected to be
# pre-installed. These are additional packages needed for calibration,
# optimization, and validation.

# Quality metrics (PSNR, SSIM, LPIPS) — needed by validate.py, smoke test
pyiqa>=0.1.12

# Semantic prompt diversity — needed by prompt_loader when
# prompt_selection is "semantic_diversity" (80 MB model, CPU only)
sentence-transformers>=3.0

# Numba JIT — optional acceleration for the offline optimizer
# (gracefully degrades to pure-Python fallback when not installed)
numba>=0.60

# Lossless compression for refiner latent data (plan Task 2b).
# The recording pipeline falls back to raw bf16 storage when missing.
blosc2>=2.0

# Core numerical dependencies (already in ComfyUI, listed for completeness)
numpy>=1.26
torch>=2.4

# ── Latent refiner / corrector training (plan Task 6a, Task 8) ──────────
# safetensors: corrector checkpoint format (Task 4c/6i)
# tqdm: training progress bars
# pytorch-optimizer: AdEMAMix / Schedule-Free AdamW (Sophia is implemented
#   in train_corrector.py per plan deep-dive §1.2 — Gauss-Newton-Bartlett)
safetensors>=0.4
tqdm>=4.60
pytorch-optimizer>=3.0

# Optional probe extras (plan Task 3a) — lossless/lossy codec candidates.
# zfpy is already used by refiner_data.py when installed (codec 2).
zfpy>=0.5.5
fpzip>=1.0
